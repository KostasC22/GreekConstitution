<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Splash theme — applied by manifest, consumed by core-splashscreen -->
    <style name="Theme.GreekConstitution.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher_round</item>
        <item name="postSplashScreenTheme">@style/Theme.GreekConstitution</item>
    </style>

    <!-- Main app theme (unchanged) -->
    <style name="Theme.GreekConstitution" parent="Theme.Material3.DayNight.NoActionBar" />
</resources>
